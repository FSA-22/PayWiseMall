import { TopNavbar } from './TopNavbar';
import { BottomNavbar } from './BottomNavbar';

export const Header = () => {
  return (
    <header className="w-full">
      <TopNavbar />
      <BottomNavbar />
    </header>
  );
};
